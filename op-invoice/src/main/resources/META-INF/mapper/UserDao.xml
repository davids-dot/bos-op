<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luoge.bos.invoice.org.UserDao">
    <sql id="cols">
        <trim>
            id, org_id, name, mobile, email, username, passwd, status, admin, create_time, update_time
        </trim>
    </sql>

    <select id="getUser" resultType="com.luoge.bos.invoice.entity.uc.UserDO">
        select
        <include refid="cols"/>
        from iam_user
        where org_id = #{id}
        and admin = #{admin}
    </select>

    <insert id="insert" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        insert into iam_user(org_id, name, mobile, email, username, passwd, status, admin, create_time, update_time)
        values (#{orgId}, #{name}, #{mobile}, #{email}, #{username}, #{passwd}, #{status}, #{admin}, #{createTime},
                #{updateTime})
    </insert>

    <update id="update">
        update iam_user
        set name        = #{name},
            mobile      = #{mobile},
            email       = #{email},
            username    = #{username},
            update_time = #{updateTime}
        where org_id = #{orgId}
          and admin = 1
    </update>

    <update id="resetPassword">
        update iam_user
        set passwd = #{passwd}
        where org_id = #{orgId}
          and admin = 1
    </update>

    <select id="existName" resultType="boolean">
        select count(*)
        from iam_user
        where username = #{username}
    </select>

    <select id="getByName" resultType="com.luoge.bos.invoice.entity.uc.UserDO">
        select
        <include refid="cols"/>
        from iam_user
        where username = #{username}
    </select>

    <select id="getByMobile" resultType="com.luoge.bos.invoice.entity.uc.UserDO">
        select
        <include refid="cols"/>
        from iam_user
        where mobile = #{mobile}
    </select>

    <select id="getByEmail" resultType="com.luoge.bos.invoice.entity.uc.UserDO">
        select
        <include refid="cols"/>
        from iam_user
        where email = #{email}
    </select>

</mapper>